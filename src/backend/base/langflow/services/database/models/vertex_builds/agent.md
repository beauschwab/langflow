# models/vertex_builds/ — Vertex Build Database Model

## Purpose
SQLModel and CRUD operations for vertex build results — stores build timing, outputs, and status for each vertex execution.

## Key Files

| File | Description |
|------|-------------|
| `model.py` | `VertexBuildModel` — records individual vertex build results. |
| `crud.py` | CRUD operations for vertex build records. |

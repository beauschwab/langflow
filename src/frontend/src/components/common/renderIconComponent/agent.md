# renderIconComponent/ — Icon Renderer with Fallbacks

## Purpose
Renders icons with fallback behavior — tries custom icons first, then Lucide icons, then a default.

## Subdirectories

| Folder | Description |
|--------|-------------|
| `components/` | Icon rendering sub-components. |

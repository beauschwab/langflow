# src/ — Custom Docusaurus Components

## Purpose
Custom React components and theme overrides for the Docusaurus documentation site.

## Subdirectories
| Folder | Description |
|--------|-------------|
| `components/` | Custom React components used in documentation pages. |
| `theme/` | Docusaurus theme overrides. |

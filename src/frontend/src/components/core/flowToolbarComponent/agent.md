# flowToolbarComponent/ — Flow Editor Toolbar

## Purpose
Toolbar at the top/bottom of the flow editor with run, undo, redo, save, playground, and view controls.

## Subdirectories

| Folder | Description |
|--------|-------------|
| `components/` | Toolbar sub-components. |

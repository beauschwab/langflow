# components/custom_component/ — Custom Component

## Purpose
The "Custom Component" meta-component that allows users to write arbitrary Python code as a Langflow component directly in the flow editor.

## Key Files

| File | Description |
|------|-------------|
| `custom_component.py` | `CustomComponent` — accepts user Python code, validates it, and executes it as a component. |

# Stores

Zustand state stores for global application state. Key stores: flowStore (current flow), flowsManagerStore (all flows), authStore (auth state), typesStore (component types), alertStore (notifications), foldersStore (folder tree), messagesStore (chat messages), darkStore (theme).

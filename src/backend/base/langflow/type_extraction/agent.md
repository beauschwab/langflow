# type_extraction/ — Runtime Type Extraction

## Purpose
Extracts type information from Python objects at runtime for component introspection. Used to determine input/output types from component class definitions.

## Key Files

| File | Description |
|------|-------------|
| `type_extraction.py` | Type extraction functions — inspects Python type annotations, generic types, and return types to build the component type system. |

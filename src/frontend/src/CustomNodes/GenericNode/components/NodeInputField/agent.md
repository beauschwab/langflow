# NodeInputField/ — Node Input Field

## Purpose
Renders an individual input parameter field on the node, delegating to the appropriate input component (text, dropdown, file, etc.) based on the field type.

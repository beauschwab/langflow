# API

API client layer using React Query (TanStack Query) for data fetching, mutations, and caching. Subdirs: queries/ (query hooks by domain), services/ (raw API functions), helpers/ (shared utilities).

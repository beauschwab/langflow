# models/transactions/ — Transaction Database Model

## Purpose
SQLModel and CRUD operations for vertex build transaction records, used for monitoring and debugging flow execution.

## Key Files

| File | Description |
|------|-------------|
| `model.py` | `TransactionModel` — records each vertex build with timing, status, and error info. |
| `crud.py` | CRUD operations for transaction records. |

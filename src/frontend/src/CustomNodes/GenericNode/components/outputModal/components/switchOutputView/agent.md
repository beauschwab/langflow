# switchOutputView/ — Output View Switcher

## Purpose
Component for switching between different output display formats (raw text, JSON, table, DataFrame).

## Subdirectories

| Folder | Description |
|--------|-------------|
| `components/` | Individual view format components. |
| `helpers/` | View switching helper functions. |

# NodeStatus/ — Build Status Indicator

## Purpose
Displays the current build status of a node — idle, building (spinner), success (checkmark), or error (red indicator).

## Subdirectories

| Folder | Description |
|--------|-------------|
| `components/` | Status sub-components. |
| `utils/` | Status utility functions. |

# Utils

Shared utility functions — React Flow helpers (reactflowUtils.ts), style utilities (styleUtils.ts), build utilities (buildUtils.ts), layout utilities (layoutUtils.ts), string manipulation, local/session storage helpers.

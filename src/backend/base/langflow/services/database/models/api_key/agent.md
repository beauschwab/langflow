# models/api_key/ — API Key Database Model

## Purpose
SQLModel and CRUD operations for user API keys used for programmatic access.

## Key Files

| File | Description |
|------|-------------|
| `model.py` | `ApiKeyModel` — SQLModel class for the API keys table. |
| `crud.py` | CRUD operations — create, read, revoke API keys. |

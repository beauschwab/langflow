# tests/integration/ — Integration Tests

## Purpose
Integration tests that exercise full application flows, require external services, or test cross-module interactions.

## Folder Structure

| Folder / File | Description |
|---------------|-------------|
| `backward_compatibility/` | Tests ensuring backward compatibility with older flow formats. |
| `components/` | Integration tests for specific components that require external services. |
| `flows/` | End-to-end flow execution tests. |
| `utils.py` | Integration test utility functions. |

# models/user/ — User Database Model

## Purpose
SQLModel and CRUD operations for user accounts.

## Key Files

| File | Description |
|------|-------------|
| `model.py` | `UserModel` — SQLModel class for the users table. Stores username, hashed password, role, active status. |
| `crud.py` | CRUD operations — create, authenticate, update, delete users. |

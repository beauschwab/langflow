# tests/locust/ — Load Tests

## Purpose
Load testing scripts using the Locust framework for stress testing the Langflow API under high concurrency.

## For LLM Coding Agents

- Run with `locust -f src/backend/tests/locust/<file>.py` to execute load tests.
